# 포트폴리오 글 작성 지침

## 문체 원칙

- 실제 개발자가 업무와 구현 내용을 설명하는 자연스러운 한국어로 작성한다.
- 추상적인 평가 대신 무엇을 설계·구현·수정했는지 구체적인 사실을 쓴다.
- 기능의 효과를 설명할 때는 근거가 확인된 결과만 쓴다. 수치나 검증 자료가 없다면 과장된 평가를 붙이지 않는다.
- 같은 의미를 제목, 요약, 본문에서 반복하지 않는다.
- 문장을 불필요하게 길게 연결하지 않고, 한 문장에는 하나의 핵심 내용을 둔다.
- `개선`, `최적화`, `표준화`, `주도`, `지원`처럼 범위가 넓은 단어만으로 성과를 설명하지 않는다. 변경 전후의 구조, 처리 방식 또는 담당 범위를 함께 쓴다.
- `전반`, `전체`, `다양한`, `주요`, `핵심` 같은 포괄적인 표현은 뒤에 구체적인 대상을 나열할 때만 사용한다.
- 프로젝트 소개와 담당 기능, 주요 성과에 같은 작업을 반복해서 쓰지 않는다. 소개에는 범위를, 담당 기능에는 작업을, 성과에는 확인된 결과를 쓴다.
- `설계 및 구현`, `관리 및 개선`처럼 명사를 나열하기보다 실제 행동을 문장으로 풀어 쓴다.
- 수정 내용을 보고할 때 `처리 분리`, `내용 분리`, `행동 명시`, `전환 명시`처럼 목적어와 명사만 이어 붙이지 않는다. 실제로 무엇을 어떻게 고쳤는지 완결된 문장으로 설명한다.
- `명시`, `분리`, `추가`, `정리`를 작업 설명의 끝말로 반복하지 않는다. 코드 구조를 설명할 때도 각 클래스나 기능이 맡는 일을 직접 쓴다.
- `역할과 책임을 나눈다`처럼 설계 원칙을 번역한 듯한 표현을 사용하지 않는다. 실제로 공통화한 기능, 재사용한 코드 또는 변경 범위를 설명한다.
- 어색한 표현을 없애기 위해 소개 내용을 지나치게 줄이지 않는다. 경력 소개에는 프로젝트 단계별 담당 업무, 구조를 설계한 방식과 문제를 해결한 과정을 충분히 남긴다.

## 피해야 할 표현

- `명확하게`, `명확한`은 원칙적으로 사용하지 않는다. 무엇이 어떻게 달라졌는지 직접 쓴다.
- `경험`은 원칙적으로 사용하지 않는다. `플레이`, `조작`, `화면`, `과정`, `업무`, `결과`처럼 문맥에 맞는 구체적인 단어로 바꾼다.
- `유지보수성`은 사용하지 않는다. 문맥에 따라 `수정하기 쉬운 구조`, `변경 범위`, `관리하기 쉬운 코드`처럼 실제 의미가 드러나는 말로 바꾼다.
- 근거 없이 `효과적으로`, `효율적으로`, `직관적으로`, `원활하게`, `안정적으로` 같은 평가성 부사를 사용하지 않는다.
- `몰입감`, `극대화`, `최적의`, `유기적으로`, `체계적으로`, `고도화`처럼 내용 없이 성과를 강조하는 표현을 피한다.
- `~할 수 있도록 했습니다`, `~을 제공합니다`, `~에 기여했습니다`처럼 대상을 흐리는 표현보다 실제로 수행한 작업을 주어와 동사가 드러나게 쓴다.
- `역량을 축적했습니다`, `노력합니다`, `중요하게 생각합니다`, `~을 지향합니다` 같은 자기평가성 문구를 피하고 실제 행동이나 작업 방식으로 바꾼다.
- `재미를 구현했습니다`, `하나의 흐름으로 연결했습니다`, `사용 흐름을 개선했습니다`처럼 결과를 확인하기 어려운 문구는 구체적인 입력, 처리, 출력으로 바꾼다.
- `광고 흐름 구현`, `전투 흐름 구현`처럼 `~흐름 구현`으로 작업을 묶어 표현하지 않는다. 실제로 연결한 기능과 처리 결과를 직접 쓴다.
- 측정 자료가 없는 `성능 향상`, `안정성 개선`, `생산성 증가`, `체감상 N배` 등의 성과를 쓰지 않는다. 수치가 검증된 경우에만 기준과 결과를 함께 적는다.
- 한글 문장에서는 `8년차`보다 `8년 차`처럼 의존 명사를 띄어 쓴다.

## 개인 프로젝트 구성

- 개인 프로젝트 글은 `프로젝트 개요`, `주요 구현`, `개발 포인트` 순서로 구성한다.
- 개요 문단에는 프로젝트 목적과 구현 범위를 2문장 이내로 쓴다.
- 주요 구현에는 실제로 완성한 기능만 항목별로 쓴다.
- 개발 포인트에는 각 클래스가 맡는 기능, 데이터 흐름과 호출 시점 등 코드나 프로젝트에서 확인되는 내용만 쓴다.
- 마지막 문단은 앞 내용을 홍보 문구로 반복하지 않고, 구현한 기능이 어떻게 연결되는지만 간결하게 정리한다.

## 작성 후 확인

- 글을 완료하기 전에 위 표현을 검색하고, 사용 근거가 없으면 구체적인 구현 내용으로 고친다.
- 자연스럽게 소리 내어 읽었을 때 실무자가 평소 사용하지 않을 문구가 있으면 간결하게 다시 쓴다.
